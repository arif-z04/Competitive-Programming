#include <bits/stdc++.h>

using namespace std;
typedef long long int ll;
int main(){
    
    ll x, y;
    cin >> x >> y;
    ll sum = (x % 10) + (y % 10);
    cout << sum << endl;

    return 0;
}