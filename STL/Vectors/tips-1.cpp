#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector <int> v;
    v.reserve(1000);

    /*
    When the sizeo f the vector is known in 
    advance use .reserve(n) to prevent 
    unnecessary reallocations.   
    */

    
    return 0;
}