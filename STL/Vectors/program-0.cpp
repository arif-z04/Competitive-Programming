#include <bits/stdc++.h>

using namespace std;

int main()
{
    // initializing 
    vector <int> v; // empty vector of ints
    vector <int> v(5); // vector with 5 default-initialized elements
    vector <int> v(5, 10); // vector with 5 elements, each initialized to 10
    vector <int> v2(v);  // copy of vector v
    vector <int> v = {1, 2, 3}; // vector with initializer list
} 